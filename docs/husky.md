# Husky - Git Hooks для автоматизації

## Що таке Pre-commit Hooks?

**Pre-commit hooks** — це кастомні скрипти, які виконуються автоматично перед кожним комітом у Git. Вони дозволяють автоматично перевіряти та форматувати код перед тим, як він потрапить у репозиторій.

### Основні переваги:

- ✅ **Автоматична перевірка** - код перевіряється перед комітом
- ✅ **Консистентність** - весь код у репозиторії відповідає стандартам
- ✅ **Економія часу** - не потрібно вручну запускати перевірки
- ✅ **Запобігання помилкам** - помилки виявляються до коміту
- ✅ **Зручне налаштування** - проста конфігурація через Husky

## Що таке Husky?

**Husky** — це інструмент, який спрощує роботу з Git hooks. Він надає можливість конфігурувати хуки зручно та швидко, без необхідності вручну редагувати скрипти в `.git/hooks/`.

### Переваги Husky:

- ✅ **Проста конфігурація** - налаштування через JSON/JS файли
- ✅ **Кросплатформенність** - працює на Windows, macOS, Linux
- ✅ **Інтеграція з npm** - легко інтегрується з npm скриптами
- ✅ **Підтримка команди** - всі налаштування в репозиторії
- ✅ **lint-staged** - працює тільки зі зміненими файлами

## Встановлення Husky

### 1. Встановлення залежностей

```bash
npm install --save-dev husky lint-staged
```

### 2. Ініціалізація Husky

```bash
npx husky install
```

Або додайте до `package.json`:

```json
{
  "scripts": {
    "prepare": "husky install"
  }
}
```

Це автоматично ініціалізує Husky після `npm install`.

### 3. Створення pre-commit hook

```bash
npx husky add .husky/pre-commit "npx lint-staged"
```

## Конфігурація lint-staged

Створіть файл `.lintstagedrc.json` в корені проєкту:

```json
{
  "*.{ts,js}": ["eslint --fix", "prettier --write"],
  "*.{json,md}": ["prettier --write"]
}
```

### Пояснення конфігурації:

- **`*.{ts,js}`** - для TypeScript та JavaScript файлів запускається ESLint та Prettier
- **`*.{json,md}`** - для JSON та Markdown файлів запускається тільки Prettier
- **`eslint --fix`** - автоматично виправляє помилки ESLint
- **`prettier --write`** - форматує код з Prettier

## Як це працює?

1. Ви робите зміни в коді та додаєте їх до staging area (`git add`)
2. Ви намагаєтеся зробити коміт (`git commit`)
3. **Pre-commit hook автоматично запускається**
4. `lint-staged` знаходить всі змінені файли
5. Для кожного файлу запускаються відповідні команди (ESLint, Prettier)
6. Якщо все успішно - коміт створюється
7. Якщо є помилки - коміт блокується, потрібно виправити помилки

### Приклад роботи:

```bash
$ git add src/index.ts
$ git commit -m "Add new feature"

# Pre-commit hook запускається автоматично:
✔ Running tasks for staged files...
✔ eslint --fix
✔ prettier --write
✔ Linting and formatting complete!

[main abc1234] Add new feature
```

## Структура файлів Husky

Після ініціалізації Husky створюється структура:

```
.husky/
├── _/
│   └── husky.sh
└── pre-commit
```

Файл `.husky/pre-commit` містить:

```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged
```

## Альтернативи: Git Hooks без Husky

### Що таке нативні Git Hooks?

Git має вбудовану підтримку hooks через папку `.git/hooks/`. Це скрипти, які виконуються на різних етапах роботи з Git.

### Типи Git Hooks:

- **`pre-commit`** - виконується перед створенням коміту
- **`post-commit`** - виконується після створення коміту
- **`pre-push`** - виконується перед push
- **`pre-rebase`** - виконується перед rebase
- І багато інших...

### Створення pre-commit hook вручну

1. Створіть файл `.git/hooks/pre-commit`:

```bash
#!/bin/sh

# Запуск ESLint
npx eslint --fix --ext .ts,.js .

# Запуск Prettier
npx prettier --write "**/*.{ts,js,json,md}"

# Якщо команди завершилися з помилкою, блокуємо коміт
if [ $? -ne 0 ]; then
  echo "❌ Pre-commit hook failed. Please fix the errors."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
```

2. Зробіть файл виконуваним:

```bash
chmod +x .git/hooks/pre-commit
```

### Приклад bash скрипту для pre-commit

```bash
#!/bin/sh

echo "🔍 Running pre-commit checks..."

# Перевірка TypeScript файлів
echo "📝 Checking TypeScript files..."
npx eslint --fix --ext .ts src/
if [ $? -ne 0 ]; then
  echo "❌ ESLint found errors. Commit aborted."
  exit 1
fi

# Форматування файлів
echo "✨ Formatting files..."
npx prettier --write "src/**/*.{ts,js,json}"

# Перевірка тестів (опціонально)
echo "🧪 Running tests..."
npm test
if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Commit aborted."
  exit 1
fi

echo "✅ All checks passed!"
exit 0
```

### Порівняння Husky vs Нативні Git Hooks

| Критерій                  | Husky                       | Нативні Git Hooks                           |
| ------------------------- | --------------------------- | ------------------------------------------- |
| **Простота налаштування** | ✅ Дуже просто              | ❌ Потрібно знати bash/sh                   |
| **Кросплатформенність**   | ✅ Працює всюди             | ⚠️ Потрібні різні скрипти для Windows       |
| **Версіонування**         | ✅ Файли в репозиторії      | ❌ Файли в `.git/hooks/` (не версіонуються) |
| **Командна робота**       | ✅ Всі мають однакові hooks | ❌ Кожен налаштовує сам                     |
| **lint-staged**           | ✅ Вбудована підтримка      | ⚠️ Потрібно налаштувати вручну              |
| **Залежність**            | ⚠️ Потрібен npm пакет       | ✅ Без залежностей                          |
| **Гнучкість**             | ✅ JSON/JS конфігурація     | ✅ Будь-який скрипт                         |

### Коли використовувати Husky?

✅ **Використовуйте Husky, якщо:**

- Працюєте в команді розробників
- Хочете простоту налаштування
- Потрібна кросплатформенність
- Хочете версіонувати hooks у репозиторії
- Використовуєте lint-staged

### Коли використовувати нативні Git Hooks?

✅ **Використовуйте нативні hooks, якщо:**

- Працюєте самотужки
- Потрібна максимальна гнучкість
- Не хочете додаткових залежностей
- Маєте досвід з bash/sh скриптами
- Потрібні складні кастомні перевірки

## Додаткові можливості

### Pre-push Hook

Створіть hook для перевірки перед push:

```bash
npx husky add .husky/pre-push "npm test"
```

### Commit-msg Hook

Перевірка формату повідомлення коміту:

```bash
npx husky add .husky/commit-msg 'npx --no -- commitlint --edit "$1"'
```

### Post-commit Hook

Виконання дій після коміту:

```bash
npx husky add .husky/post-commit "echo 'Commit successful!'"
```

## Налаштування для команди

### 1. Додайте `.husky` до репозиторію

```bash
git add .husky
git commit -m "Add husky hooks"
```

### 2. Усі розробники отримають hooks автоматично

Після `npm install` hooks будуть встановлені автоматично завдяки скрипту `prepare`.

## Вирішення проблем

### Hook не виконується

1. Перевірте, що Husky ініціалізований: `npx husky install`
2. Перевірте права на виконання: `chmod +x .husky/pre-commit`
3. Перевірте, що файл `.husky/pre-commit` існує

### Помилки при виконанні

1. Перевірте, що всі залежності встановлені: `npm install`
2. Перевірте конфігурацію `.lintstagedrc.json`
3. Запустіть команди вручну для діагностики

### Пропуск hooks (не рекомендується)

Якщо потрібно пропустити hooks (тільки в екстрених випадках):

```bash
git commit --no-verify -m "Emergency commit"
```

⚠️ **Увага:** Не використовуйте `--no-verify` регулярно, це порушує процес контролю якості коду.

## Додаткові ресурси

- [Офіційна документація Husky](https://typicode.github.io/husky/)
- [lint-staged документація](https://github.com/lint-staged/lint-staged)
- [Git Hooks документація](https://git-scm.com/docs/githooks)

## Поради

1. **Завжди тестуйте hooks локально** - перевіряйте перед комітом
2. **Не пропускайте hooks** - використовуйте `--no-verify` тільки в екстрених випадках
3. **Додавайте hooks до репозиторію** - щоб всі в команді мали однакові налаштування
4. **Починайте з простих правил** - додавайте складні перевірки поступово
5. **Інформуйте команду** - якщо змінюєте hooks, повідомте команду
